import { useNavigate } from 'react-router-dom';

const Reccomendation = ({ setLoading, loading }) => {
    const navigate = useNavigate();



    return (
        <p>Hello</p>       
    );
}

export default Reccomendation;